<% if @gossip !=nil%>
  <div class="alert alert-danger m-0 p-3 text-center" role="alert">
      <% @gossip.errors.each do |attr, msg|  %>
      <%= attr %> <%= msg %>
    <% end %>
  </div>
  <% end %>

  <%= form_tag url_for(action: 'create'), method: "post" do %>
      <input type="hidden" name="authenticity_token" value= <%= form_authenticity_token %> >

    <div class="form-group">
      <%= label_tag 'Titre' %>
    </br>
      <%= text_field_tag 'gossip_title', value = nil, placeholder: 'Donnes un titre à ton gossip' %>

    </div>


    <div class="form-group">
      <%= label_tag 'Racontes les bails' %>
      </br>
      <%= text_field_tag 'gossip_content', value = nil, placeholder: 'Balances ton histoire ici' %>
    </div>

    <%= submit_tag "B*tcher" %>

    <% end %>
